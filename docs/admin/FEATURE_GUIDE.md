# Admin 新功能开发指南

1. 确认对应 API、权限代码和共享契约已存在。
2. 判断页面属于列表、详情、表单、设置或权限模式，复用已有页面结构。
3. 阅读 `COMPONENTS.md`，只使用 Admin 组件，不复制 Web 组件。
4. 同步菜单分组、路由、面包屑、按钮权限和中文状态映射。
5. 接入真实 API，覆盖加载、空态、错误、无权限和重复提交。
6. 在常规桌面、高分辨率桌面和窄屏下检查布局。
7. 执行 `pnpm ui:check`、Admin 类型检查和相关测试。

如果功能不需要新页面，例如只增加操作权限或配置字段，不应为了“完整”创建无价值页面。
