# ADR-0001：采用 Monorepo 与共享契约

- 状态：已接受
- 日期：2026-08-01

## 背景

后台、用户端和 API 需要独立部署，同时由 AI 和开发者保持接口一致。

## 决策

使用 pnpm workspace + Turborepo。跨端只共享无框架依赖的 TypeScript 契约；API 仍是业务规则权威来源。

## 后果

- 可以原子化修改契约和消费者，并统一执行检查。
- 必须防止应用间深层导入和把服务端模型泄漏给客户端。
- 未来若拆分仓库，应由 OpenAPI 生成的版本化包替代 workspace 直接引用。
