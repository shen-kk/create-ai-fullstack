# ADR-0007：用户验证码由 API 统一消费服务配置

## 状态

已采纳。

## 决策

用户端浏览器只调用验证码业务 API，不读取短信、邮件或 Redis 密钥。API 从后台加密服务配置中读取运行时凭据，并负责发送、频率限制、验证码摘要存储、有效期、错误次数和单次消费。

开发内存模式可在响应中返回一次性 `developmentCode` 便于零依赖验收；生产环境禁止返回验证码。腾讯云短信和 TLS SMTP 是首批真实适配器，其他供应商在适配器落地前必须明确拒绝。
