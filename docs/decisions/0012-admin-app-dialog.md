# Admin 统一弹窗组件

## 决定

后台新增弹窗统一使用 `apps/admin/src/components/AppDialog.vue`，由组件集中处理遮罩、固定头部/内容/底部结构、尺寸变体和禁止点击遮罩关闭。

## 原因

现有后台历史页面仍有分散弹窗结构；直接复制会继续产生间距、滚动和关闭行为不一致。该组件作为收口入口，后续逐步迁移历史页面。

## 范围

仅适用于 Admin，不改变用户端 Dialog 方案。组件内部使用统一的 `dialog-backdrop` 容器，页面不得再自行实现同类结构。
