# 业务词汇表

所有团队成员和 AI 使用同一术语。新增业务概念时先在此定义，避免同义词漂移。

| 术语  | 定义                                 | 禁用别名               |
| ----- | ------------------------------------ | ---------------------- |
| Admin | 使用内部后台的运营人员；不是普通用户 | 管理用户（含义模糊）   |
| User  | 使用用户端产品的最终用户             | 客户（除非领域明确）   |
| API   | `apps/api` 暴露的 HTTP 服务          | 后端接口层（含义模糊） |

具体业务领域建立独立文件，并记录状态、状态转换、权限和不可破坏的不变量。
