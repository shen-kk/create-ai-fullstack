# Web 动效规范

- VueUse Motion 负责常规入场、显隐、反馈和状态切换。
- GSAP 只负责 Hero、滚动叙事等高价值编排，使用 `gsap.context()` 并在卸载时 `revert()`。
- 同一区域要么统一采用入场节奏，要么保持静态，不只给个别同级卡片添加动画。
- 动画不能承载业务状态，关闭动画后流程仍完整可用。
- 尊重 `prefers-reduced-motion`；SSR 首屏内容不能等待客户端动画后才出现。
- 常规反馈保持短、平滑；不使用长动画拖延提交、导航或错误展示。
