{
  "name": "@template/web",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "nuxt dev --port 3002",
    "build": "nuxt build",
    "lint": "eslint nuxt.config.ts --max-warnings=0",
    "typecheck": "nuxt typecheck",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@template/contracts": "workspace:*",
    "nuxt": "^4.0.0",
    "vue": "^3.5.0"
  },
  "devDependencies": {
    "@template/config": "workspace:*",
    "vue-tsc": "^3.0.0"
  }
}
